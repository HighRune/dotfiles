local cmd = vim.cmd
local fn = vim.fn
local g = vim.g
local map = vim.keymap.set
local call = vim.call
local api = vim.api
local silent = { silent = true }
local expr = { expr = true }
local remap = { remap = true }

return {}
